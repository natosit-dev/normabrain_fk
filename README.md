This repository utilizes git submodules. To clone, use the command git clone --recurse-submodules <project url>

Requires: python3, conda, singularity

Installation: conda env create -f workflow/envs/snakemake.yaml